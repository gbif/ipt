<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE urlrewrite PUBLIC "-//tuckey.org//DTD UrlRewrite 3.0//EN"
    "http://tuckey.org/res/dtds/urlrewrite3.0.dtd">

<urlrewrite>
    <rule>
        <from>^/admin/user/(.*).html$</from>
        <to type="forward">/admin/editUser.html\?id=$1&amp;from=list</to>
    </rule>

    <!-- Override default validation.js from WebWork -->
    <rule>
        <from>^/struts/css_xhtml/validation.js$</from>
        <to type="forward">/template/css_xhtml/validation.js</to>
    </rule>

    <!-- The following fixes a bug in XFire:        -->
    <!-- http://jira.codehaus.org/browse/XFIRE-1089 -->
    <rule>
        <from>^//services/(.*)$</from>
        <to type="forward">/services/$1</to>
    </rule>


    <!-- PROVIDERTOOL RULES -->
    
    <!-- data dir -->
    <rule>
        <from>^/data/(\d+)/data.zip$</from>
        <to type="forward">/data.html?resource_id=$1</to>
    </rule>
    <!-- details -->
    <rule>
        <from>^/data/(\d+)/(.+)$</from>
        <to type="forward">/detail.html?resource_id=$1&amp;guid=$2</to>
    </rule>
    <!-- tapir -->
    <!-- 
    <rule>
        <from>^/tapir/(\d+)/?(.*)$</from>
        <to type="forward">/tapir/service.html?resource_id=$1&amp;$2</to>
    </rule>
    -->
</urlrewrite>

